# 🔧 Исправление проблем с деплоем Vercel

## Проблема
Деплой в Vercel падает с ошибкой, хотя локальная сборка проходит успешно.

## Решение

### 1. Проверьте настройки проекта в Vercel

1. Зайдите на https://vercel.com
2. Откройте проект `beauty-school-two` (или ваш проект)
3. Перейдите в **Settings** → **General**

### 2. Проверьте Root Directory

**КРИТИЧНО:** Убедитесь что:
- **Root Directory:** `frontend` (не пусто!)
- Если пусто или указан корень проекта - измените на `frontend`

### 3. Проверьте Build & Development Settings

В **Settings** → **General** → **Build & Development Settings**:

- **Framework Preset:** `Vite`
- **Build Command:** `npm run build` (или оставьте пустым - Vercel определит автоматически)
- **Output Directory:** `dist`
- **Install Command:** `npm install` (или оставьте пустым)

### 4. Проверьте переменные окружения

В **Settings** → **Environment Variables**:

Убедитесь что есть:
- `VITE_API_URL` = `https://your-backend-url.railway.app/api` (ваш Railway backend URL)

### 5. Пересоздайте деплой

После изменения настроек:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Или создайте новый деплой: **"Deploy"** → **"Redeploy"**

### 6. Проверьте логи деплоя

Если деплой все еще падает:

1. Откройте последний деплой
2. Нажмите на шаг где произошла ошибка
3. Посмотрите логи - там будет точная ошибка

## Частые проблемы

### Ошибка: "Root Directory not found"
**Решение:** Установите Root Directory = `frontend` в настройках проекта

### Ошибка: "Build command failed"
**Решение:** 
- Проверьте что `package.json` находится в папке `frontend`
- Проверьте что все зависимости установлены
- Попробуйте изменить Build Command на `cd frontend && npm run build` (если Root Directory не установлен)

### Ошибка: "Output Directory not found"
**Решение:** Убедитесь что Output Directory = `dist` и что сборка создает эту папку

## Альтернативное решение: Использовать Vercel CLI

Если веб-интерфейс не помогает:

```bash
cd frontend
vercel --prod
```

Это задеплоит напрямую через CLI, обходя настройки проекта.

