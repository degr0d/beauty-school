# 🔧 Исправление ошибки: "The provided path frontend/frontend does not exist"

## Проблема

Ошибка возникает потому что:
- Вы запускаете `vercel` из папки `frontend`
- Но в настройках проекта Vercel установлен Root Directory = `frontend`
- Vercel пытается найти `frontend/frontend` → ошибка!

## Решение

### Вариант 1: Убрать Root Directory в настройках Vercel (рекомендуется)

1. Зайдите на https://vercel.com/dimas-projects-c69016ee/beauty-school/settings
2. **General** → **Root Directory**
3. **Очистите поле** (оставьте пустым)
4. Сохраните

После этого запустите снова:
```bash
cd frontend
vercel --prod
```

### Вариант 2: Запустить из корня проекта

Если хотите оставить Root Directory = `frontend`:

```bash
cd /Users/goose/.cursor/worktrees/beauty/fDMJa  # Корень проекта
vercel --prod --cwd frontend
```

Или:
```bash
cd /Users/goose/.cursor/worktrees/beauty/fDMJa
vercel --prod
# И укажите Root Directory = frontend при настройке
```

### Вариант 3: Использовать vercel.json

Убедитесь что в `frontend/vercel.json` нет указания на Root Directory (его там и нет, это правильно).

## Рекомендация

**Лучше всего:** Убрать Root Directory в настройках Vercel, так как вы всегда деплоите из папки `frontend`.

После исправления настройки, запустите:
```bash
cd frontend
vercel --prod
```

