# 🛠️ Локальная разработка без Telegram

## Проблема
Приложение требует регистрации через Telegram бота, но для локальной разработки в браузере это неудобно.

## Решение: Режим разработки

### Шаг 1: Убедитесь что ENVIRONMENT=development

В `.env` файле должно быть:
```bash
ENVIRONMENT=development
```

### Шаг 2: Запустите backend

```bash
python run_api.py
```

Backend должен запуститься на `http://localhost:8000`

### Шаг 3: Настройте frontend для локальной разработки

В режиме разработки frontend автоматически добавит заголовок `X-Telegram-User-ID` с вашим админским ID.

**Вариант A: Использовать админский ID по умолчанию**

Просто откройте frontend в браузере - он автоматически использует первый админский ID из `ADMIN_IDS` в `.env`.

**Вариант B: Указать конкретный telegram_id**

Откройте консоль браузера (F12) и выполните:
```javascript
localStorage.setItem('dev_telegram_id', '310836227'); // Ваш telegram_id
```

Или добавьте в `frontend/src/api/client.ts` автоматическую установку заголовка для разработки.

### Шаг 4: Запустите frontend

```bash
cd frontend
npm run dev
```

Frontend откроется на `http://localhost:5173`

### Шаг 5: Откройте в браузере

Просто откройте `http://localhost:5173` - приложение должно работать!

## Как это работает

В режиме разработки (`ENVIRONMENT=development`):
- Backend пропускает проверку Telegram initData
- Использует `X-Telegram-User-ID` из заголовка (если есть)
- Или использует первый админский ID по умолчанию
- Frontend автоматически добавляет заголовок с вашим telegram_id

## Альтернатива: Использовать реальный Telegram Mini App

Если хотите протестировать с реальным Telegram:

1. Запустите ngrok или другой туннель:
```bash
ngrok http 5173
```

2. Обновите `.env`:
```bash
WEBAPP_URL=https://your-ngrok-url.ngrok.io
```

3. Перезапустите бота
4. Откройте Mini App через бота

## Проверка

После запуска:
1. Откройте `http://localhost:5173`
2. Откройте консоль браузера (F12)
3. Проверьте что запросы к API проходят успешно
4. Профиль должен загрузиться автоматически

