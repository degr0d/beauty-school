# 🤖 Настройка админ-бота на Railway

## Важно!

Админ-бот - это **ОТДЕЛЬНЫЙ бот**, который должен работать параллельно с основным ботом.

На Railway нужно создать **ТРЕТИЙ сервис** для админ-бота.

---

## Текущая структура на Railway

У вас должно быть:
- ✅ Сервис `web` (API) - `python run_api.py`
- ✅ Сервис `bot` (Основной бот) - `python run_bot_production.py`
- ❌ Сервис `admin-bot` (Админ-бот) - **НУЖНО СОЗДАТЬ**

---

## Создание сервиса для админ-бота

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на [railway.app](https://railway.app)
2. Откройте ваш проект

### Шаг 2: Создайте новый сервис для админ-бота

1. В вашем проекте нажмите **"+ New"** (или **"New Service"**)
2. Выберите **"GitHub Repo"**
3. Выберите тот же репозиторий: `beauty-school` (или `degr0d/beauty-school`)
4. Назовите сервис: `admin-bot` (или `beauty-school-admin-bot`)

### Шаг 3: Настройте Start Command

1. Откройте созданный сервис `admin-bot`
2. Перейдите в **Settings**
3. Найдите **"Start Command"** (или **"Deploy"** → **"Start Command"**)
4. Установите: `python run_admin_bot_production.py`
5. Сохраните изменения

### Шаг 4: Добавьте переменные окружения

В сервисе `admin-bot` → **Variables** → добавьте:

```
ADMIN_BOT_TOKEN=8591343240:AAHn5pfTtCERWY1jl0MXseCQPECpa7_4Jz8
ADMIN_IDS=310836227
ENVIRONMENT=production
```

**Важно:** Railway автоматически подключит `DATABASE_URL` из PostgreSQL сервиса.

### Шаг 5: Подключите к базе данных

1. В сервисе `admin-bot` → **Variables**
2. Нажмите **"+ Add Variable"**
3. Выберите **"Add Reference"**
4. Выберите **Postgres** сервис
5. Выберите переменную `DATABASE_URL`
6. Сохраните

### Шаг 6: Проверьте деплой

1. Railway автоматически задеплоит новый сервис
2. Проверьте вкладку **"Logs"** в сервисе `admin-bot`
3. Должны увидеть:
   ```
   ✅ Админ-бот запущен: @your_admin_bot_name
   🚀 Админ-бот готов к работе!
   ```

### Шаг 7: Проверьте работу админ-бота

1. Откройте админ-бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить
4. Попробуйте команду `/seed_data` для создания тестовых данных

---

## Итоговая структура на Railway

После настройки у вас должно быть:

- ✅ Сервис `web` (API) - `python run_api.py`
- ✅ Сервис `bot` (Основной бот) - `python run_bot_production.py`
- ✅ Сервис `admin-bot` (Админ-бот) - `python run_admin_bot_production.py`
- ✅ Сервис `Postgres` (Database)
- ✅ Все сервисы подключены к одной БД

---

## Troubleshooting

### Админ-бот не отвечает

1. **Проверьте логи:**
   - Railway → сервис `admin-bot` → **Logs**
   - Ищите ошибки

2. **Проверьте ADMIN_BOT_TOKEN:**
   - Railway → сервис `admin-bot` → **Variables**
   - Убедитесь что `ADMIN_BOT_TOKEN` правильный
   - Проверьте в @BotFather

3. **Проверьте что админ-бот запущен:**
   - В логах должно быть: `✅ Админ-бот запущен: @your_admin_bot_name`

### Ошибка подключения к БД

1. Убедитесь что `DATABASE_URL` подключен через Reference
2. Проверьте что PostgreSQL сервис запущен

### Команды не работают

1. Проверьте что ваш Telegram ID в `ADMIN_IDS`
2. Админ-бот доступен только пользователям из `ADMIN_IDS`

---

## Готово! 🎉

После этого админ-бот будет работать 24/7 на Railway параллельно с основным ботом!


