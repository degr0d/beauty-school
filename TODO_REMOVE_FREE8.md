# ⚠️ ВАЖНО: УДАЛИТЬ ВРЕМЕННУЮ КОМАНДУ /free8

## 📋 Информация

**Команда:** `/free8`  
**Файл:** `backend/bot/handlers/commands.py`  
**Функция:** `cmd_free8` (строки ~238-315)  
**Дата добавления:** 2025-11-08

## 🎯 Что делает

Команда `/free8` выдает доступ ко всем курсам пользователю, который её вызвал.  
Система будет считать, что пользователь купил все курсы.

## ⚠️ ВАЖНО: УДАЛИТЬ ПОСЛЕ ТЕСТИРОВАНИЯ!

Эта команда является временной и должна быть удалена после завершения тестирования.

## 🔍 Как удалить

1. Откройте файл: `backend/bot/handlers/commands.py`
2. Найдите блок с комментарием:
   ```python
   # ========================================
   # ⚠️ ВРЕМЕННАЯ КОМАНДА - УДАЛИТЬ ПОСЛЕ ТЕСТИРОВАНИЯ!
   # ========================================
   @router.message(Command("free8"))
   async def cmd_free8(message: Message):
       ...
   ```
3. Удалите весь блок (включая комментарии)
4. Также удалите импорты, если они больше не используются:
   - `from datetime import datetime` (если только для этой команды)
   - `UserCourse` из `backend.database` (если только для этой команды)

## 📝 Проверка перед удалением

- [ ] Тестирование завершено
- [ ] Команда больше не нужна
- [ ] Все пользователи протестированы

## 🗑️ После удаления

Удалите этот файл (`TODO_REMOVE_FREE8.md`) после удаления команды.



