# 🔍 Пошаговая диагностика проблем с Vercel

## Шаг 1: Откройте последний деплой с ошибкой

1. Зайдите на https://vercel.com
2. Откройте проект `beauty-school` (или `degr0d/beauty-school`)
3. Вкладка **Deployments**
4. Найдите последний деплой с красным крестиком (Error)
5. **Кликните на него** чтобы открыть детали

## Шаг 2: Посмотрите логи ошибки

В деталях деплоя:

1. Найдите шаг где произошла ошибка (обычно "Building" или "Installing")
2. **Кликните на этот шаг**
3. Прокрутите логи вниз
4. Найдите строки с `Error:` или `Failed:`
5. **Скопируйте текст ошибки** (хотя бы последние 10-20 строк)

## Шаг 3: Проверьте настройки проекта

1. В Vercel проекте → **Settings** → **General**
2. Проверьте:
   - **Root Directory:** должно быть `frontend` (НЕ пусто!)
   - **Framework Preset:** `Vite`
   - **Build Command:** `npm run build` (или пусто)
   - **Output Directory:** `dist`
   - **Install Command:** `npm install` (или пусто)

## Шаг 4: Проверьте подключение к GitHub

1. **Settings** → **Git**
2. Убедитесь что:
   - Репозиторий подключен: `degr0d/beauty-school`
   - Production Branch: `main`
   - Автоматический деплой включен

## Шаг 5: Попробуйте принудительный деплой через CLI

Если веб-интерфейс не работает:

```bash
cd frontend
npm install -g vercel
vercel login
vercel --prod
```

Это задеплоит напрямую, обходя настройки проекта.

## Частые ошибки и решения

### "Root Directory not found"
**Решение:** Settings → General → Root Directory = `frontend`

### "Build command failed: npm run build"
**Решение:** 
- Проверьте что `package.json` в папке `frontend`
- Проверьте логи - там будет точная ошибка TypeScript или сборки

### "Cannot find module"
**Решение:** 
- Убедитесь что `node_modules` не в `.gitignore` (но он должен быть)
- Проверьте что все зависимости в `package.json`

### "Output Directory 'dist' not found"
**Решение:**
- Проверьте что сборка создает папку `dist`
- Проверьте Build Command: `npm run build`

## Что делать дальше

После того как найдете точную ошибку в логах:
1. Скопируйте текст ошибки
2. Пришлите мне - я помогу исправить

