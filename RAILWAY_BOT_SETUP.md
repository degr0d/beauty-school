# 🤖 Настройка бота на Railway

## Проблема
Бот не работает, потому что на Railway запущен только API сервис. Нужно создать отдельный сервис для бота.

## Решение: Создать второй сервис на Railway

### Шаг 1: Откройте Railway Dashboard
1. Зайдите на [railway.app](https://railway.app)
2. Откройте ваш проект `blissful-encouragement`

### Шаг 2: Создайте новый сервис для бота

1. В вашем проекте нажмите **"+ New"** (или **"New Service"**)
2. Выберите **"GitHub Repo"**
3. Выберите тот же репозиторий: `beauty-school` (или `degr0d/beauty-school`)
4. Назовите сервис: `bot` (или `beauty-school-bot`)

### Шаг 3: Настройте Start Command

1. Откройте созданный сервис `bot`
2. Перейдите в **Settings**
3. Найдите **"Start Command"** (или **"Deploy"** → **"Start Command"**)
4. Установите: `python run_bot_production.py`
5. Сохраните изменения

### Шаг 4: Добавьте переменные окружения

В сервисе `bot` → **Variables** → добавьте те же переменные, что и в сервисе `web`:

```
BOT_TOKEN=your_bot_token
ADMIN_BOT_TOKEN=your_admin_bot_token
ADMIN_IDS=1003457491,310836227
ENVIRONMENT=production
FRONTEND_URL=https://beauty-school-two.vercel.app
WEBAPP_URL=https://beauty-school-two.vercel.app
SECRET_KEY=your_secret_key
```

**Важно:** Railway автоматически подключит `DATABASE_URL` из PostgreSQL сервиса.

### Шаг 5: Подключите к базе данных

1. В сервисе `bot` → **Variables**
2. Нажмите **"+ Add Variable"**
3. Выберите **"Add Reference"**
4. Выберите **Postgres** сервис
5. Выберите переменную `DATABASE_URL`
6. Сохраните

### Шаг 6: Проверьте деплой

1. Railway автоматически задеплоит новый сервис
2. Проверьте вкладку **"Logs"** в сервисе `bot`
3. Должны увидеть:
   ```
   ✅ Бот запущен: @your_bot_name
   🚀 Бот готов к работе!
   ```

### Шаг 7: Проверьте работу бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить

## Проверка

После настройки у вас должно быть:

- ✅ Сервис `web` (API) - `python run_api.py`
- ✅ Сервис `bot` (Bot) - `python run_bot_production.py`
- ✅ Сервис `Postgres` (Database)
- ✅ Оба сервиса подключены к одной БД

## Troubleshooting

### Бот не отвечает

1. **Проверьте логи:**
   - Railway → сервис `bot` → **Logs**
   - Ищите ошибки

2. **Проверьте BOT_TOKEN:**
   - Railway → сервис `bot` → **Variables**
   - Убедитесь что `BOT_TOKEN` правильный
   - Проверьте в @BotFather

3. **Проверьте что бот запущен:**
   - В логах должно быть: `✅ Бот запущен: @your_bot_name`

### Ошибка подключения к БД

1. Убедитесь что `DATABASE_URL` подключен через Reference
2. Проверьте что PostgreSQL сервис запущен

## Готово! 🎉

После этого бот будет работать 24/7 на Railway!

