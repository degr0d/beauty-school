# 🚀 Автоматический деплой

После внесения изменений в код, вы можете автоматически задеплоить их на Railway (Backend) и Vercel (Frontend).

## Быстрый старт

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
# Из корня проекта
./scripts/auto_deploy.sh
```

Скрипт автоматически:
1. ✅ Проверит изменения
2. ✅ Создаст коммит (можно указать сообщение)
3. ✅ Отправит в GitHub (push в main)
4. ✅ Railway и Vercel автоматически задеплоят

**С сообщением коммита:**
```bash
./scripts/auto_deploy.sh "Исправлена проблема с профилем пользователя"
```

### Вариант 2: Вручную через git

```bash
# 1. Добавить изменения
git add -A

# 2. Создать коммит
git commit -m "Описание изменений"

# 3. Отправить в GitHub
git push origin main
```

Railway и Vercel автоматически задеплоят при push в `main`!

---

## Как это работает

### Railway (Backend)
- ✅ Автоматически деплоит при push в `main` ветку
- ✅ Использует `Procfile` для определения команд запуска
- ✅ Запускает два сервиса: API и Bot

### Vercel (Frontend)
- ✅ Автоматически деплоит при push в `main` ветку
- ✅ Использует `frontend/vercel.json` для конфигурации
- ✅ Собирает frontend и деплоит

### GitHub Actions
- ✅ Проверяет код перед деплоем
- ✅ Собирает frontend для проверки
- ✅ Логирует изменения

---

## Проверка статуса деплоя

### Railway
1. Зайдите на https://railway.app
2. Выберите ваш проект
3. Проверьте вкладку **Deployments**

### Vercel
1. Зайдите на https://vercel.com
2. Выберите ваш проект
3. Проверьте вкладку **Deployments**

---

## Troubleshooting

### Ошибка: "Permission denied"
```bash
chmod +x scripts/auto_deploy.sh
```

### Ошибка: "Not a git repository"
Убедитесь что вы в корне проекта и инициализирован git:
```bash
git init
git remote add origin <your-repo-url>
```

### Railway не деплоит
1. Проверьте что репозиторий подключен в Railway
2. Проверьте что ветка `main` отслеживается
3. Проверьте логи в Railway → Deployments

### Vercel не деплоит
1. Проверьте что репозиторий подключен в Vercel
2. Проверьте что Root Directory = `frontend`
3. Проверьте логи в Vercel → Deployments

---

## Полезные команды

### Проверить статус git
```bash
git status
```

### Посмотреть последние коммиты
```bash
git log --oneline -5
```

### Отменить последний коммит (если еще не запушен)
```bash
git reset --soft HEAD~1
```

### Принудительный деплой на Railway
```bash
railway up
```

### Принудительный деплой на Vercel
```bash
cd frontend
vercel --prod
```

---

## Безопасность

⚠️ **Важно:** Скрипт автоматически делает commit и push. Убедитесь что:
- ✅ Все изменения протестированы локально
- ✅ Нет секретных данных в коде
- ✅ `.env` файлы в `.gitignore`

---

## Готово! 🎉

Теперь после каждого изменения просто запустите:
```bash
./scripts/auto_deploy.sh
```

И Railway + Vercel автоматически задеплоят ваши изменения!

