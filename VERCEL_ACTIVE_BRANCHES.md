# 🌿 Active Branches в Vercel - Что это?

## Что такое Active Branches?

**Active Branches** - это preview деплои для разных веток Git. Vercel автоматически создает отдельный деплой для каждой ветки, чтобы можно было протестировать изменения до merge в main.

## Что вы видите

В вашем проекте есть две активные ветки:

1. **`2025-11-06-f1b3-xREpH`** - красный кружок (ошибка деплоя)
2. **`2025-11-06-s9rz-AzAvR`** - зеленый/голубой кружок (успешный деплой)

## Что делать с этими ветками?

### Вариант 1: Удалить старые ветки (рекомендуется)

Эти ветки старые (1 день назад) и больше не нужны. Можно удалить:

1. В Vercel → **Overview** → **Active Branches**
2. Нажмите **"..."** (три точки) справа от ветки
3. Выберите **"Delete"** или **"Remove"**

Или удалите ветки в GitHub:
```bash
git push origin --delete 2025-11-06-f1b3-xREpH
git push origin --delete 2025-11-06-s9rz-AzAvR
```

### Вариант 2: Оставить как есть

Если не мешают - можно оставить. Vercel автоматически удалит старые preview деплои через некоторое время.

## Главное - Production деплой

**Важно:** Вам нужен деплой для ветки `main` (Production), а не для этих preview веток.

Проверьте:
1. Вкладка **Deployments**
2. Найдите деплой для ветки **`main`**
3. Если его нет или он с ошибкой - нужно исправить настройки проекта

## Что проверить для Production деплоя

1. **Settings** → **Git**:
   - **Production Branch:** должен быть `main`
   - **Connected Git Repository:** `degr0d/beauty-school`

2. **Settings** → **General**:
   - **Root Directory:** должен быть `frontend`

После исправления, следующий push в `main` должен автоматически создать Production деплой.

