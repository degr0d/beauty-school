# 🚀 Деплой на Vercel

## Быстрый старт

### 1. Установите Vercel CLI

```bash
npm i -g vercel
```

### 2. Войдите в Vercel

```bash
vercel login
```

Выберите способ входа:
- ✅ **Continue with Google** (если заходили через Google)
- ✅ **Continue with GitHub** (если заходили через GitHub)
- ✅ **Continue with Email** (если заходили через email)

### 3. Деплой frontend

```bash
cd frontend
vercel
```

Следуйте инструкциям:
- ✅ **Link to existing project?** → `N` (создаем новый)
- ✅ **What's your project's name?** → `beauty-school-frontend` (или любое другое)
- ✅ **In which directory is your code located?** → `./` (текущая папка frontend)
- ✅ **Want to override the settings?** → `N`

### 4. Настройте переменные окружения

После первого деплоя зайдите на [vercel.com](https://vercel.com) → ваш проект → Settings → Environment Variables

Добавьте:
- `VITE_API_URL` = `https://your-backend-url.com/api` (URL вашего backend API)

**Важно:** Backend должен быть доступен по HTTPS!

### 5. Обновите .env в корне проекта

После деплоя Vercel даст вам URL типа `https://beauty-school-frontend.vercel.app`

Обновите `.env`:
```bash
WEBAPP_URL=https://beauty-school-frontend.vercel.app
```

### 6. Перезапустите бота

После обновления `.env` перезапустите бота, чтобы он использовал новый URL.

---

## Автоматический деплой через Git

### 1. Подключите GitHub репозиторий

1. Зайдите на [vercel.com](https://vercel.com)
2. New Project → Import Git Repository
3. Выберите ваш репозиторий
4. **Root Directory:** выберите `frontend`
5. **Framework Preset:** Vite
6. **Build Command:** `npm run build`
7. **Output Directory:** `dist`
8. **Install Command:** `npm install`

### 2. Настройте переменные окружения

В настройках проекта добавьте:
- `VITE_API_URL` = URL вашего backend API

### 3. Деплой

После каждого push в `main` ветку Vercel автоматически задеплоит новую версию!

---

## Проверка деплоя

После деплоя проверьте:
1. ✅ Frontend открывается: `https://your-app.vercel.app`
2. ✅ API запросы работают (проверьте в консоли браузера)
3. ✅ Telegram Mini App открывается через бота

---

## Troubleshooting

### Ошибка: "API URL not found"

**Решение:** Проверьте что `VITE_API_URL` установлен в настройках Vercel проекта.

### Backend не доступен

**Решение:** Backend должен быть задеплоен и доступен по HTTPS. Можно использовать:
- Railway
- Render
- Fly.io
- Или свой сервер с SSL

### Telegram Mini App не открывается

**Решение:** 
1. Проверьте что `WEBAPP_URL` в `.env` указывает на Vercel URL
2. Перезапустите бота
3. Проверьте что URL начинается с `https://`

