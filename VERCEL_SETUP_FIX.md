# 🔧 Исправление: Vercel не деплоит автоматически

## Проблема
GitHub Actions проходит успешно, но Vercel не запускает автоматический деплой при push в main.

## Решение

### Шаг 1: Проверьте подключение репозитория

1. Зайдите на https://vercel.com
2. Откройте проект `beauty-school`
3. **Settings** → **Git**
4. Проверьте:
   - **Connected Git Repository:** должен быть `degr0d/beauty-school`
   - **Production Branch:** должен быть `main`
   - **Auto-deploy:** должен быть включен (ON)

### Шаг 2: Проверьте Root Directory

**КРИТИЧЕСКОЕ:** Это самая частая причина!

1. **Settings** → **General**
2. Найдите **Root Directory**
3. Должно быть: `frontend` (НЕ пусто!)
4. Если пусто или указан корень проекта:
   - Нажмите **Edit**
   - Введите: `frontend`
   - Сохраните

### Шаг 3: Проверьте Build Settings

В **Settings** → **General** → **Build & Development Settings**:

- **Framework Preset:** `Vite` (или `Other`)
- **Build Command:** `npm run build` (или оставьте пустым)
- **Output Directory:** `dist`
- **Install Command:** `npm install` (или оставьте пустым)

### Шаг 4: Переподключите репозиторий (если нужно)

Если настройки правильные, но деплой не запускается:

1. **Settings** → **Git**
2. Нажмите **Disconnect** (отключить репозиторий)
3. Затем **Connect Git Repository**
4. Выберите `degr0d/beauty-school`
5. Настройте:
   - **Root Directory:** `frontend`
   - **Framework Preset:** `Vite`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
6. Сохраните

### Шаг 5: Принудительный деплой

После исправления настроек:

1. Перейдите в **Deployments**
2. Нажмите **"Deploy"** → **"Deploy from GitHub"**
3. Выберите ветку `main`
4. Или используйте CLI:

```bash
cd frontend
vercel --prod
```

### Шаг 6: Проверьте переменные окружения

В **Settings** → **Environment Variables**:

Убедитесь что есть:
- `VITE_API_URL` = `https://your-backend-url.railway.app/api`

## Частые проблемы

### Vercel не видит push в main
**Решение:** Проверьте что Production Branch = `main` в Settings → Git

### Деплой не запускается автоматически
**Решение:** 
- Проверьте что Auto-deploy включен
- Переподключите репозиторий
- Проверьте что Root Directory = `frontend`

### "Root Directory not found"
**Решение:** Установите Root Directory = `frontend` в Settings → General

## Альтернатива: Деплой через CLI

Если веб-интерфейс не помогает:

```bash
cd frontend
npm install -g vercel
vercel login
vercel link  # Подключить к существующему проекту
vercel --prod  # Деплой в продакшн
```

Это задеплоит напрямую, обходя проблемы с автоматическим деплоем.

