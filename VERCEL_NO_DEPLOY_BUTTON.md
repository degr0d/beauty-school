# 🔧 Нет кнопки деплоя в Vercel - Решение

## Проблема
В Vercel нет кнопки "Deploy" и автоматический деплой не работает.

## Решение

### Шаг 1: Проверьте подключение репозитория

1. Зайдите на https://vercel.com/dimas-projects-c69016ee/beauty-school/settings
2. Вкладка **Git**
3. Проверьте:
   - **Connected Git Repository:** должен быть `degr0d/beauty-school`
   - Если написано "Not connected" или репозиторий другой - нужно переподключить

### Шаг 2: Переподключите репозиторий (если не подключен)

1. В **Settings** → **Git**
2. Если репозиторий не подключен:
   - Нажмите **"Connect Git Repository"**
   - Выберите `degr0d/beauty-school`
   - При настройке укажите:
     - **Root Directory:** `frontend`
     - **Framework Preset:** `Vite`
     - **Build Command:** `npm run build`
     - **Output Directory:** `dist`
   - Сохраните

### Шаг 3: Проверьте Root Directory

**КРИТИЧЕСКОЕ:** Это самая частая причина!

1. **Settings** → **General**
2. Найдите **Root Directory**
3. Должно быть: `frontend` (НЕ пусто!)
4. Если пусто:
   - Нажмите **Edit**
   - Введите: `frontend`
   - Сохраните

### Шаг 4: Проверьте Production Branch

1. **Settings** → **Git**
2. **Production Branch:** должен быть `main`
3. Если не `main` - измените на `main`

### Шаг 5: Проверьте Auto-deploy

1. **Settings** → **Git**
2. **Auto-deploy:** должен быть **ON** (включен)
3. Если выключен - включите

### Шаг 6: Принудительный деплой через веб-интерфейс

Если все настройки правильные, но деплой не запускается:

1. Зайдите на https://vercel.com/dimas-projects-c69016ee/beauty-school
2. Вкладка **Deployments**
3. Найдите последний деплой (даже с ошибкой)
4. Нажмите на **"..."** (три точки) справа от деплоя
5. Выберите **"Redeploy"**

Или:

1. В правом верхнем углу страницы Deployments найдите кнопку **"..."** (три точки)
2. Там должна быть опция **"Deploy"** или **"Deploy from GitHub"**

### Шаг 7: Если кнопки все равно нет

Попробуйте создать новый проект:

1. Зайдите на https://vercel.com/new
2. **Import Git Repository**
3. Выберите `degr0d/beauty-school`
4. При настройке:
   - **Root Directory:** `frontend`
   - **Framework Preset:** `Vite`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
5. Сохраните

После этого удалите старый проект `beauty-school` (если нужно).

## Альтернатива: Деплой через CLI (если настроите доступ)

Если настроите доступ к команде Vercel:

```bash
cd frontend
vercel --prod
```

Но лучше использовать автоматический деплой через GitHub - это проще.

## Что проверить в первую очередь

1. **Root Directory = `frontend`** (самая частая проблема!)
2. **Репозиторий подключен** (`degr0d/beauty-school`)
3. **Production Branch = `main`**
4. **Auto-deploy = ON**

После исправления этих настроек, следующий push в `main` должен автоматически запустить деплой.

