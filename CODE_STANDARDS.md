# 📋 Регламент разработки Beauty School

## 🎯 Основные принципы

### ⚠️ Важно: Регламент применяется только к НАШЕМУ коду

**Регламент НЕ применяется к:**
- ❌ `node_modules/` - зависимости (npm пакеты)
- ❌ `venv/` - виртуальное окружение Python
- ❌ `__pycache__/` - скомпилированный Python код
- ❌ `dist/`, `build/` - сгенерированные файлы сборки
- ❌ Файлы миграций (Alembic) - могут быть большими
- ❌ Сгенерированные конфигурации

**Регламент применяется к:**
- ✅ `backend/` - наш Python код
- ✅ `frontend/src/` - наш TypeScript/React код
- ✅ `scripts/` - наши скрипты
- ✅ Конфигурационные файлы проекта

### 1. Размер файлов

**Python (Backend):**
- ✅ **Максимум 200 строк** на файл
- ✅ **Идеально: 100-150 строк**
- ⚠️ Если файл > 200 строк → разбить на модули

**TypeScript/React (Frontend):**
- ✅ **Максимум 250 строк** на компонент
- ✅ **Идеально: 150-200 строк**
- ⚠️ Если компонент > 250 строк → разбить на подкомпоненты

**Utils (Утилиты):**
- ✅ **Максимум 150 строк** на утилиту
- ✅ **Идеально: 50-100 строк**
- ⚠️ Если утилита > 150 строк → разбить на модули

**Обоснование:**
- Легче читать и понимать
- Проще тестировать
- Меньше конфликтов в Git
- Соответствует PEP 8 и React best practices

---

## 📁 Структура проекта

### Backend (Python)

```
backend/
├── bot/                    # Telegram бот
│   ├── handlers/          # Обработчики (макс 150 строк каждый)
│   │   ├── start.py       # Команда /start
│   │   ├── registration.py # Регистрация (если >200 строк → разбить)
│   │   └── commands.py   # Другие команды
│   ├── keyboards.py       # Клавиатуры (макс 100 строк)
│   └── states.py          # FSM состояния (макс 50 строк)
├── admin_bot/             # Админ-бот
│   └── handlers/          # Обработчики (макс 150 строк)
├── webapp/                # FastAPI API
│   ├── routes/           # Эндпоинты (макс 200 строк каждый)
│   ├── schemas.py        # Pydantic схемы (макс 200 строк)
│   └── middleware.py     # Middleware (макс 150 строк)
├── database/             # БД
│   ├── models.py         # Модели (макс 300 строк, иначе разбить)
│   └── migrations/       # Миграции Alembic
├── services/             # Бизнес-логика (макс 150 строк каждый)
└── utils/                # Утилиты (макс 150 строк каждый, идеально 50-100)
```

### Frontend (React + TypeScript)

```
frontend/src/
├── pages/                # Страницы (макс 250 строк)
├── components/           # Компоненты (макс 200 строк)
├── api/                 # API клиент (макс 200 строк)
├── hooks/               # Custom hooks (макс 100 строк)
├── utils/               # Утилиты (макс 150 строк, идеально 50-100)
└── styles/              # Стили
```

---

## 🔧 Правила организации кода

### 1. Один файл = одна ответственность

**❌ Плохо:**
```python
# handlers.py (500 строк)
# - регистрация
# - команды
# - сообщения
# - калбеки
```

**✅ Хорошо:**
```python
# handlers/
#   ├── registration.py (150 строк) - только регистрация
#   ├── commands.py (100 строк) - только команды
#   └── messages.py (80 строк) - только сообщения
```

### 2. Разбиение больших функций

**❌ Плохо:**
```python
def process_user_registration(...):  # 100 строк
    # шаг 1 (20 строк)
    # шаг 2 (30 строк)
    # шаг 3 (25 строк)
    # шаг 4 (25 строк)
```

**✅ Хорошо:**
```python
def process_user_registration(...):
    data = await validate_input(...)
    user = await create_user(...)
    await send_welcome_message(...)
    return user

async def validate_input(...):  # 20 строк
async def create_user(...):     # 15 строк
async def send_welcome_message(...):  # 10 строк
```

### 3. Разделение на сервисы

**❌ Плохо:**
```python
# routes/courses.py (300 строк)
# - получение курсов
# - подсчет прогресса
# - валидация
# - форматирование
```

**✅ Хорошо:**
```python
# routes/courses.py (100 строк) - только эндпоинты
# services/courses.py (80 строк) - бизнес-логика
# utils/validators.py (50 строк) - валидация
```

---

## 📏 Метрики качества

### Комплексность (Cyclomatic Complexity)
- ✅ **Максимум 10** на функцию
- ⚠️ Если > 10 → разбить на несколько функций

### Глубина вложенности
- ✅ **Максимум 4 уровня** вложенности
- ⚠️ Если > 4 → вынести в отдельную функцию

### Длина строк
- ✅ **Максимум 100 символов** (PEP 8 рекомендует 79-99)
- ⚠️ Если > 100 → перенести на новую строку

---

## 🎨 Стиль кода

### Python
- ✅ Следуем **PEP 8**
- ✅ Используем **type hints** везде
- ✅ **Docstrings** для всех функций/классов
- ✅ **Black** форматирование (длина строки 100)

### TypeScript/React
- ✅ Следуем **ESLint** правилам
- ✅ **TypeScript strict mode**
- ✅ **Functional components** с hooks
- ✅ **Named exports** вместо default

---

## 🔍 Проверка перед коммитом

### Автоматические проверки:
1. ✅ **Linter** (flake8/pylint для Python, ESLint для TS)
2. ✅ **Formatter** (black для Python, prettier для TS)
3. ✅ **Type checker** (mypy для Python, tsc для TS)

### Ручные проверки:
1. ✅ Размер файлов не превышает лимиты
2. ✅ Функции не слишком сложные
3. ✅ Нет дублирования кода
4. ✅ Документация актуальна

---

## 📝 Документация

### Правила организации документации:

**⚠️ ВАЖНО: Вся документация должна быть в папке `docs/`!**

- ✅ **Все `.md` файлы** (кроме `README.md` в корне) → `docs/`
- ✅ **Структурированная организация** - каждый тип документации в своём файле
- ✅ **Объединение дублей** - не создавать несколько файлов с одинаковой темой
- ✅ **README.md в корне** - только основная информация и ссылки на `docs/`

### Структура `docs/`:

```
docs/
├── README.md              # Навигация по документации
├── quickstart.md          # Быстрый старт
├── mac-setup.md           # Настройка на Mac
├── tunnel-guide.md        # Туннель для Mini App
├── miniapp-troubleshooting.md  # Решение проблем Mini App
├── mvp-status.md          # Статус MVP и следующие шаги
├── project-status.md      # Статус проекта и оптимизация
├── ai-workflow.md         # Работа с AI (как не прерывать)
├── architecture.md        # Архитектура проекта
├── database.md            # Схема базы данных
├── deployment.md          # Деплой
└── testing-guide.md       # Тестирование
```

### Обязательно документировать:
- ✅ Все публичные функции/классы
- ✅ Сложную бизнес-логику
- ✅ API эндпоинты (FastAPI автоматически)
- ✅ Типы данных (TypeScript)

### Формат docstring (Python):
```python
def process_registration(user_id: int, data: dict) -> User:
    """
    Обрабатывает регистрацию пользователя.
    
    Args:
        user_id: Telegram ID пользователя
        data: Данные регистрации (ФИО, телефон)
    
    Returns:
        Созданный объект User
    
    Raises:
        ValueError: Если данные некорректны
    """
```

---

## 🚫 Что НЕ делать

### ❌ Монолитные файлы
- Не создавать файлы > 300 строк
- Не объединять несвязанную функциональность

### ❌ Дублирование кода
- Не копировать код между файлами
- Выносить общую логику в утилиты/сервисы

### ❌ Глубокая вложенность
- Не делать > 4 уровней вложенности
- Использовать early return

### ❌ Магические числа/строки
```python
# ❌ Плохо
if status == 42:
    ...

# ✅ Хорошо
STATUS_ACTIVE = 42
if status == STATUS_ACTIVE:
    ...
```

---

## ✅ Чеклист перед коммитом

- [ ] Все **НАШИ** файлы < 200 строк (Python) / 250 строк (TS) / 150 строк (Utils)
- [ ] **Исключены** из проверки: node_modules, venv, dist, build, __pycache__
- [ ] Функции < 50 строк
- [ ] Нет дублирования кода
- [ ] Type hints везде (Python)
- [ ] Docstrings для публичных функций
- [ ] Линтер проходит без ошибок
- [ ] Форматтер применен
- [ ] Тесты проходят (если есть)

---

## 🔄 Рефакторинг больших файлов

### Когда файл > 200 строк:

1. **Определить ответственности**
   - Что делает этот файл?
   - Какие части можно выделить?

2. **Разбить на модули**
   - Создать отдельные файлы для каждой ответственности
   - Вынести общую логику в сервисы/утилиты

3. **Обновить импорты**
   - Проверить все места использования
   - Обновить импорты в зависимых файлах

4. **Проверить работоспособность**
   - Запустить тесты
   - Проверить вручную

---

## 📚 Ссылки на стандарты

- **PEP 8** - Python Style Guide
- **Google Python Style Guide**
- **React Best Practices**
- **TypeScript Style Guide**
- **FastAPI Best Practices**

---

**Версия:** 1.0  
**Дата:** 2025-01-04  
**Статус:** Активен

