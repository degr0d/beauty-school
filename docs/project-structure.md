# 📁 Структура проекта

```
beauty/
│
├── 📱 backend/                      # Python Backend (Bot + API)
│   ├── bot/                         # Telegram Bot
│   │   ├── handlers/               # Обработчики команд
│   │   │   ├── start.py           # /start
│   │   │   ├── registration.py    # Регистрация (FSM)
│   │   │   ├── commands.py        # /help, /profile, /courses
│   │   │   └── webapp.py          # Web App callbacks
│   │   ├── bot.py                 # Настройка bot dispatcher
│   │   ├── keyboards.py           # Клавиатуры
│   │   └── states.py              # FSM states
│   │
│   ├── webapp/                      # FastAPI REST API
│   │   ├── routes/                # API endpoints
│   │   │   ├── courses.py        # GET /api/courses
│   │   │   ├── lessons.py        # GET /api/lessons/:id
│   │   │   ├── profile.py        # GET /api/profile
│   │   │   └── progress.py       # POST /api/progress
│   │   ├── app.py                # FastAPI app
│   │   ├── middleware.py         # Auth middleware
│   │   └── schemas.py            # Pydantic models
│   │
│   ├── database/                    # База данных
│   │   ├── migrations/            # Alembic миграции
│   │   │   ├── versions/         # Версии миграций
│   │   │   └── env.py            # Alembic config
│   │   ├── models.py             # SQLAlchemy models
│   │   ├── database.py           # DB connection
│   │   └── seed_data.py          # Тестовые данные
│   │
│   ├── admin_bot/                   # Админ-бот
│   │   ├── handlers/              # Админ команды
│   │   └── bot.py                # Admin bot setup
│   │
│   ├── services/                    # Бизнес-логика
│   │   ├── auth.py               # Аутентификация
│   │   ├── courses.py            # Курсы
│   │   └── progress.py           # Прогресс
│   │
│   ├── utils/                       # Утилиты
│   │   ├── logger.py             # Логирование
│   │   └── validators.py        # Валидация
│   │
│   ├── config.py                   # Конфигурация
│   └── main.py                     # Главный файл запуска
│
├── 🎨 frontend/                     # React + TypeScript (Mini App)
│   ├── src/
│   │   ├── api/                   # API client
│   │   │   └── client.ts         # Axios + auth
│   │   ├── components/            # React компоненты
│   │   │   ├── CourseCard.tsx
│   │   │   ├── LessonItem.tsx
│   │   │   ├── Navigation.tsx
│   │   │   └── ProgressBar.tsx
│   │   ├── pages/                 # Страницы
│   │   │   ├── MainPage.tsx      # Главная
│   │   │   ├── CoursesPage.tsx   # Каталог курсов
│   │   │   ├── CoursePage.tsx    # Страница курса
│   │   │   ├── LessonPage.tsx    # Урок
│   │   │   ├── ProfilePage.tsx   # Профиль
│   │   │   └── CommunitiesPage.tsx
│   │   ├── hooks/                 # Custom hooks
│   │   │   └── useTelegram.ts    # Telegram Web App SDK
│   │   ├── styles/               # Стили
│   │   │   └── global.css
│   │   ├── App.tsx               # Main component
│   │   └── main.tsx              # Entry point
│   ├── package.json              # npm dependencies
│   ├── vite.config.ts            # Vite config
│   └── tsconfig.json             # TypeScript config
│
├── 📚 docs/                         # Документация
│   ├── architecture.md            # Архитектура
│   ├── database.md                # База данных
│   ├── deployment.md              # Деплой
│   ├── mvp-roadmap.md             # MVP план
│   ├── setup_ngrok.md             # Настройка ngrok
│   ├── VSCODE_ЗАПУСК.md           # Запуск в VSCode
│   └── project-structure.md       # Этот файл
│
├── 🛠️ scripts/                      # Утилиты
│   ├── check_users.py             # Проверить пользователей
│   ├── check_courses.py           # Проверить курсы
│   └── reset_user.py              # Сбросить пользователя
│
├── 🐳 Docker
│   ├── docker-compose.yml         # Dev окружение
│   ├── docker-compose.prod.yml    # Prod окружение
│   └── Dockerfile.backend         # Backend image
│
├── ⚙️ Конфигурация
│   ├── .env                       # Переменные окружения (не в git!)
│   ├── config.example             # Пример .env
│   ├── requirements.txt           # Python зависимости
│   ├── alembic.ini                # Alembic config
│   └── .gitignore                 # Git ignore rules
│
├── 🚀 Запуск (Windows)
│   ├── LAUNCH.cmd                 # Запустить всё
│   ├── STOP.cmd                   # Остановить всё
│   ├── START.ps1                  # PowerShell launcher
│   └── STOP.ps1                   # PowerShell stop
│
├── 🐍 Python runners
│   ├── run_all.py                 # Bot + API
│   ├── run_bot_full.py            # Только Bot
│   └── run_api.py                 # Только API
│
├── 📝 VSCode
│   └── .vscode/                   # Конфигурация VSCode
│       ├── launch.json            # Debug configs (F5)
│       ├── tasks.json             # Tasks
│       ├── settings.json          # Настройки
│       └── keybindings.json       # Горячие клавиши
│
└── README.md                       # Главная документация
```

---

## 🗂️ Основные папки

### `backend/`
Весь серверный код на Python:
- **bot/** - Telegram бот (aiogram)
- **webapp/** - REST API (FastAPI)
- **database/** - Модели и миграции (SQLAlchemy + Alembic)
- **admin_bot/** - Админ панель через бота
- **services/** - Бизнес-логика
- **utils/** - Вспомогательные функции

### `frontend/`
React приложение (Mini App):
- **src/pages/** - Страницы приложения
- **src/components/** - Переиспользуемые компоненты
- **src/api/** - API клиент
- **src/hooks/** - Custom React hooks

### `docs/`
Вся документация проекта

### `scripts/`
Вспомогательные скрипты для разработки

---

## 📄 Ключевые файлы

| Файл | Описание |
|------|----------|
| `.env` | Переменные окружения (токены, пароли) |
| `requirements.txt` | Python зависимости |
| `docker-compose.yml` | Docker сервисы (PostgreSQL, Redis) |
| `alembic.ini` | Конфигурация миграций БД |
| `LAUNCH.cmd` | Запуск всего проекта (Windows) |
| `run_all.py` | Запуск Bot + API |

---

## 🔗 Связи между компонентами

```
┌─────────────┐
│   Telegram  │
│   Users     │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│   Telegram Bot      │◄────── aiogram
│   (backend/bot/)    │
└──────┬──────────────┘
       │
       ├──► PostgreSQL (через SQLAlchemy)
       │
       ▼
┌─────────────────────┐
│   REST API          │◄────── FastAPI
│   (backend/webapp/) │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   Mini App          │◄────── React + Vite
│   (frontend/)       │
└─────────────────────┘
```

---

## 🛤️ User Flow

1. **Пользователь** открывает бота в Telegram
2. **/start** → **Регистрация** (FSM)
3. **Кнопка "Открыть приложение"** → **Mini App** (React)
4. **Mini App** → **REST API** (FastAPI)
5. **API** → **PostgreSQL** (данные)
6. **Ответ** → **Mini App** → **Пользователь**

---

## 🔧 Для разработчиков

### Где что править:

| Задача | Файл/папка |
|--------|------------|
| Добавить команду бота | `backend/bot/handlers/` |
| Добавить API endpoint | `backend/webapp/routes/` |
| Изменить модель БД | `backend/database/models.py` + миграция |
| Добавить страницу в Mini App | `frontend/src/pages/` |
| Изменить стили | `frontend/src/styles/global.css` |
| Конфигурация | `backend/config.py` + `.env` |

### Полезные команды:

```bash
# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Заполнить БД тестовыми данными
python -m backend.database.seed_data

# Проверить пользователей
python scripts/check_users.py
```

---

**См. также:**
- [Архитектура](architecture.md)
- [База данных](database.md)
- [Запуск в VSCode](VSCODE_ЗАПУСК.md)

