# 🔧 Локальная разработка без Telegram

## Проблема
При локальной разработке на `localhost:5173` нет Telegram WebApp, поэтому приложение не может идентифицировать пользователя.

## ✅ Решение

Режим разработки автоматически активируется на `localhost` и позволяет работать без Telegram бота.

### Как это работает:

1. **Фронтенд** (на localhost):
   - Автоматически определяет, что вы на localhost
   - Отправляет заголовок `X-Telegram-User-ID` вместо `X-Telegram-Init-Data`
   - Использует `telegram_id` из `localStorage` (по умолчанию: `123456789`)

2. **Бэкенд** (в режиме разработки):
   - Если `DEV_MODE=True` и `ENVIRONMENT=development`
   - Принимает заголовок `X-Telegram-User-ID`
   - Создает фейкового пользователя для разработки

### Как использовать:

#### Вариант 1: Через компонент DevModeSelector (рекомендуется)

1. Откройте `http://localhost:5173`
2. В правом верхнем углу появится желтая панель "🔧 Режим разработки"
3. Нажмите на `+` чтобы развернуть
4. Введите ваш Telegram ID (из базы данных или любой существующий)
5. Нажмите "💾 Сохранить и перезагрузить"
6. Готово! Приложение будет работать с указанным ID

#### Вариант 2: Через консоль браузера

```javascript
// Установить ваш Telegram ID
localStorage.setItem('dev_telegram_id', 'ВАШ_TELEGRAM_ID')

// Перезагрузить страницу
window.location.reload()
```

#### Вариант 3: Через .env файл

Добавьте в `.env`:
```bash
DEV_MODE=True
DEV_TELEGRAM_ID=123456789  # Ваш Telegram ID
```

### Как узнать свой Telegram ID:

1. **Из базы данных:**
   ```sql
   SELECT id, telegram_id, full_name FROM users;
   ```

2. **Через бота:**
   - Напишите боту `/start`
   - ID будет в логах бэкенда

3. **Через Telegram:**
   - Напишите боту @userinfobot
   - Он покажет ваш ID

### Настройки в .env:

```bash
# Режим разработки (включен по умолчанию)
DEV_MODE=True

# Telegram ID для разработки (опционально)
# Если не указан, используется из заголовка X-Telegram-User-ID
DEV_TELEGRAM_ID=123456789

# Окружение
ENVIRONMENT=development
```

### Важно:

- ✅ Режим разработки работает **только на localhost**
- ✅ В продакшене всегда требуется Telegram initData
- ✅ Middleware отключен в режиме разработки (строка 56 в `app.py`)
- ✅ Для продакшена нужно включить middleware: `app.add_middleware(TelegramAuthMiddleware)`

### Проверка:

1. Откройте `http://localhost:5173`
2. Откройте DevTools (F12 или Alt+D)
3. Перейдите на вкладку "Профиль"
4. В консоли должно быть: `🔧 [DEV MODE] Отправка запроса с X-Telegram-User-ID: ...`
5. Профиль должен загрузиться без ошибок

### Устранение проблем:

**Проблема:** Все еще ошибка 401
- **Решение:** Проверьте, что `DEV_MODE=True` в `.env`
- **Решение:** Проверьте, что middleware отключен в `app.py` (строка 56)

**Проблема:** Не вижу компонент DevModeSelector
- **Решение:** Убедитесь, что открываете `localhost:5173`, а не другой URL
- **Решение:** Очистите кеш браузера (Ctrl+Shift+R)

**Проблема:** Профиль не загружается
- **Решение:** Проверьте логи бэкенда - должно быть `🔧 [DEV MODE] Используется telegram_id...`
- **Решение:** Убедитесь, что пользователь с таким ID существует в БД (или будет создан автоматически)

