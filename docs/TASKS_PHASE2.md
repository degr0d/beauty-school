# 📋 Задачи для Phase 2 - Post-MVP функции

## 🎯 Цель
Реализовать функции из "После MVP: Full версия" согласно roadmap.

---

## 📊 Текущее состояние (из CURRENT_STATUS.md)

### ✅ MVP завершен:
- Регистрация через бота
- Каталог курсов
- Просмотр уроков
- Прогресс обучения
- Профиль пользователя
- Сообщества
- Базовый админ-бот

### ⚠️ Частично работает:
- **Оплата** - создание платежей работает, но интеграция с ЮKassa не завершена
- **Профиль** - история курсов не реализована (только заглушка)

### ❌ Отсутствует:
- Геймификация (ачивки, баллы, лидборды)
- Сертификаты PDF
- Пуш-уведомления
- Челленджи
- Детальная аналитика

---

## 🔍 Задачи для агентов

### Агент 1: Завершение оплаты и история курсов
**Цель:** Доработать критичные функции для полноценной работы оплаты

**Приоритет:** 🔴 КРИТИЧНО

**Задачи:**

1. **Завершить интеграцию с ЮKassa**
   - Раскомментировать код в `backend/webapp/routes/payment.py`
   - Настроить Configuration с реальными данными из settings
   - Реализовать обработку webhook (`/api/payment/webhook`)
   - Автоматически создавать UserCourse после успешной оплаты
   - Обновлять статус платежа при получении событий от ЮKassa
   - Протестировать полный цикл оплаты

2. **Реализовать историю курсов в профиле**
   - Использовать существующий эндпоинт `/api/courses/my/courses`
   - Обновить `ProfilePage.tsx` для отображения купленных курсов
   - Показать прогресс по каждому курсу
   - Добавить ссылки на курсы
   - Убрать заглушку "Раздел в разработке"

**Файлы для работы:**
- `backend/webapp/routes/payment.py` - доработать оплату
- `frontend/src/pages/ProfilePage.tsx` - добавить историю курсов
- `backend/config.py` - проверить настройки ЮKassa

**Результат:** 
- Оплата полностью работает
- История курсов отображается в профиле

---

### Агент 2: Геймификация (ачивки и баллы)
**Цель:** Реализовать систему достижений и начисления баллов

**Приоритет:** 🟡 ВАЖНО

**Задачи:**

1. **API для достижений**
   - `GET /api/achievements` - список всех достижений
   - `GET /api/achievements/my` - достижения пользователя
   - `GET /api/achievements/{id}` - детали достижения
   - Создать `backend/webapp/routes/achievements.py`

2. **Логика начисления баллов**
   - Начисление баллов при завершении урока
   - Начисление баллов при завершении курса
   - Начисление баллов при получении достижения
   - Обновление поля `points` в таблице `users`
   - Создать `backend/services/gamification.py`

3. **Проверка условий достижений**
   - Автоматическая проверка при завершении урока/курса
   - Создание записи в `user_achievements` при выполнении условия
   - Уведомление пользователя (через бота или в Mini App)

4. **Лидборды (топ пользователей)**
   - `GET /api/leaderboard` - топ пользователей по баллам
   - `GET /api/leaderboard/courses` - топ по завершенным курсам
   - Создать страницу `LeaderboardPage.tsx` (опционально)

**Файлы для работы:**
- `backend/webapp/routes/achievements.py` - создать новый файл
- `backend/services/gamification.py` - создать новый файл
- `backend/webapp/routes/leaderboard.py` - создать новый файл (опционально)
- `frontend/src/pages/ProfilePage.tsx` - добавить отображение достижений
- `frontend/src/api/client.ts` - добавить API методы

**Результат:**
- Система достижений работает
- Баллы начисляются автоматически
- Пользователи видят свои достижения

---

### Агент 3: Сертификаты PDF
**Цель:** Реализовать генерацию и выдачу сертификатов

**Приоритет:** 🟡 ВАЖНО

**Задачи:**

1. **Модель для сертификатов**
   - Добавить таблицу `certificates` в БД
   - Поля: user_id, course_id, issued_at, certificate_url
   - Создать миграцию Alembic

2. **Генерация PDF сертификатов**
   - Установить библиотеку (ReportLab или WeasyPrint)
   - Создать шаблон сертификата
   - Генерация при завершении курса (100% прогресс)
   - Сохранение PDF на сервере или в облаке
   - Создать `backend/services/certificates.py`

3. **API для сертификатов**
   - `GET /api/certificates` - список сертификатов пользователя
   - `GET /api/certificates/{id}` - скачать сертификат
   - `GET /api/certificates/course/{course_id}` - сертификат по курсу
   - Создать `backend/webapp/routes/certificates.py`

4. **UI для сертификатов**
   - Отобразить сертификаты в профиле
   - Кнопка "Скачать сертификат"
   - Превью сертификата (опционально)

**Файлы для работы:**
- `backend/database/models.py` - добавить модель Certificate
- `backend/database/migrations/versions/` - создать миграцию
- `backend/services/certificates.py` - создать новый файл
- `backend/webapp/routes/certificates.py` - создать новый файл
- `frontend/src/pages/ProfilePage.tsx` - добавить раздел сертификатов
- `frontend/src/api/client.ts` - добавить API методы

**Результат:**
- Сертификаты генерируются автоматически
- Пользователи могут скачать свои сертификаты

---

### Агент 4: Пуш-уведомления
**Цель:** Реализовать систему уведомлений через Telegram бота

**Приоритет:** 🟢 ЖЕЛАТЕЛЬНО

**Задачи:**

1. **Модель для уведомлений**
   - Добавить таблицу `notifications` в БД
   - Поля: user_id, type, title, message, read_at, created_at
   - Создать миграцию Alembic

2. **Сервис отправки уведомлений**
   - Создать `backend/services/notifications.py`
   - Функция отправки через Telegram Bot API
   - Типы уведомлений:
     - Новый курс доступен
     - Напоминание о незавершенных уроках
     - Получено достижение
     - Новое сообщество

3. **Расписание уведомлений**
   - Напоминания о незавершенных курсах (раз в неделю)
   - Уведомления о новых курсах
   - Использовать Celery или простой cron (опционально)

4. **API для уведомлений**
   - `GET /api/notifications` - список уведомлений пользователя
   - `PUT /api/notifications/{id}/read` - отметить как прочитанное
   - Создать `backend/webapp/routes/notifications.py`

5. **UI для уведомлений** (опционально)
   - Бейдж с количеством непрочитанных
   - Страница со списком уведомлений

**Файлы для работы:**
- `backend/database/models.py` - добавить модель Notification
- `backend/database/migrations/versions/` - создать миграцию
- `backend/services/notifications.py` - создать новый файл
- `backend/webapp/routes/notifications.py` - создать новый файл
- `backend/bot/bot.py` - добавить функцию отправки уведомлений

**Результат:**
- Пользователи получают уведомления через бота
- Есть API для работы с уведомлениями

---

### Агент 5: Челленджи
**Цель:** Реализовать систему челленджей для мотивации пользователей

**Приоритет:** 🟢 ЖЕЛАТЕЛЬНО

**Задачи:**

1. **Модель для челленджей**
   - Добавить таблицу `challenges` в БД
   - Поля: title, description, type, condition, reward_points, start_date, end_date, is_active
   - Добавить таблицу `user_challenges` для отслеживания участия
   - Создать миграции Alembic

2. **API для челленджей**
   - `GET /api/challenges` - список активных челленджей
   - `GET /api/challenges/{id}` - детали челленджа
   - `GET /api/challenges/my` - челленджи пользователя
   - `POST /api/challenges/{id}/join` - присоединиться к челленджу
   - Создать `backend/webapp/routes/challenges.py`

3. **Логика проверки выполнения**
   - Автоматическая проверка условий челленджа
   - Начисление награды при выполнении
   - Создать `backend/services/challenges.py`

4. **UI для челленджей**
   - Страница `ChallengesPage.tsx` со списком челленджей
   - Отображение прогресса выполнения
   - Кнопка "Присоединиться"

**Файлы для работы:**
- `backend/database/models.py` - добавить модели Challenge и UserChallenge
- `backend/database/migrations/versions/` - создать миграции
- `backend/services/challenges.py` - создать новый файл
- `backend/webapp/routes/challenges.py` - создать новый файл
- `frontend/src/pages/ChallengesPage.tsx` - создать новый файл
- `frontend/src/api/client.ts` - добавить API методы

**Результат:**
- Система челленджей работает
- Пользователи могут участвовать и получать награды

---

## 📝 Формат отчетов

Каждый агент должен создать файл с результатами:
- `AGENT1_PAYMENT_STATUS.md` - статус оплаты и истории курсов
- `AGENT2_GAMIFICATION_STATUS.md` - статус геймификации
- `AGENT3_CERTIFICATES_STATUS.md` - статус сертификатов
- `AGENT4_NOTIFICATIONS_STATUS.md` - статус уведомлений
- `AGENT5_CHALLENGES_STATUS.md` - статус челленджей

---

## 🚀 Как начать работу

1. Каждый агент работает в своем worktree
2. Создает ветку для своих изменений
3. Коммитит изменения с понятными сообщениями
4. Пушит в GitHub
5. Создает отчет в корне проекта

---

## ⚠️ Важно

- Следовать существующей архитектуре проекта
- Использовать существующие компоненты и стили
- Тестировать каждую функцию перед коммитом
- Обновлять документацию при добавлении новых функций
- Коммитить и пушить изменения после завершения задач

---

## 📚 Документация для справки

- `CURRENT_STATUS.md` - текущий статус проекта
- `docs/mvp-roadmap.md` - план разработки
- `docs/architecture.md` - архитектура проекта
- `docs/database.md` - структура БД
- `README.md` - общая информация

---

## 🎯 Приоритеты выполнения

**Критично (сделать первым):**
1. Агент 1 - Завершение оплаты и история курсов

**Важно (следующий этап):**
2. Агент 2 - Геймификация
3. Агент 3 - Сертификаты

**Желательно (после критичного):**
4. Агент 4 - Пуш-уведомления
5. Агент 5 - Челленджи

---

**Удачи! 🚀**

