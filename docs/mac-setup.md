# 🍎 Beauty School - Инструкция для macOS

Краткое руководство по настройке проекта на Mac.

## 🚀 Быстрый старт

### 1. Первоначальная настройка

Выполните скрипт автоматической настройки:

```bash
./setup_mac.sh
```

Этот скрипт проверит и настроит:
- ✅ Python 3 и виртуальное окружение
- ✅ .env файл (создаст из config.example если нужно)
- ✅ Python зависимости
- ✅ Node.js (установит если нужно)
- ✅ npm зависимости
- ✅ Docker (проверит наличие)

### 2. Настройка .env файла

После выполнения `setup_mac.sh`, отредактируйте `.env`:

```bash
nano .env  # или используйте любой редактор
```

**Обязательно заполните:**
- `BOT_TOKEN` - токен от @BotFather
- `ADMIN_BOT_TOKEN` - токен админ-бота
- `ADMIN_IDS` - ваш Telegram ID (через запятую)
- `DB_PASSWORD` - пароль для PostgreSQL

### 3. Запуск Docker контейнеров

```bash
docker-compose up -d
```

Проверка статуса:
```bash
docker-compose ps
```

### 4. Применение миграций базы данных

```bash
source venv/bin/activate
alembic upgrade head
```

### 5. (Опционально) Заполнение тестовыми данными

```bash
python -m backend.database.seed_data
```

### 6. Запуск проекта

```bash
./launch.sh
```

Скрипт запустит:
- ✅ Backend (Bot + API) на порту 8000
- ✅ Frontend на порту 5173
- ✅ Автоматически откроет браузер

### 7. Остановка проекта

```bash
./stop.sh
```

---

## 📋 Ручная настройка (если setup_mac.sh не подходит)

### Python окружение

```bash
# Создание venv (если нужно пересоздать)
rm -rf venv
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt
```

### Node.js

Если Node.js не установлен:

**Вариант 1: Homebrew**
```bash
brew install node
```

**Вариант 2: Официальный сайт**
- Скачайте: https://nodejs.org/

**Установка npm зависимостей:**
```bash
cd frontend
npm install
cd ..
```

### Docker

Установите Docker Desktop для Mac:
- https://www.docker.com/products/docker-desktop

Проверка:
```bash
docker --version
docker-compose --version
```

---

## 🔧 Полезные команды

### Проверка пользователей в БД
```bash
source venv/bin/activate
python scripts/check_users.py
```

### Проверка курсов
```bash
python scripts/check_courses.py
```

### Сброс пользователя
```bash
python scripts/reset_user.py
```

### Запуск только бота
```bash
source venv/bin/activate
python run_bot_full.py
```

### Запуск только API
```bash
source venv/bin/activate
python run_api.py
```

### Миграции БД
```bash
# Применить миграции
alembic upgrade head

# Создать новую миграцию
alembic revision --autogenerate -m "описание изменений"
```

---

## ⚠️ Частые проблемы

### VPN обязателен
Telegram API заблокирован без VPN. Включите VPN перед запуском!

### Виртуальное окружение не активируется
Если venv создано на Windows, пересоздайте:
```bash
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Docker не запускается
1. Проверьте, что Docker Desktop запущен
2. Проверьте статус: `docker info`

### Node.js не найден
Установите через Homebrew: `brew install node`
Или скачайте с https://nodejs.org/

### Порт занят
Если порт 8000 или 5173 занят:
- Backend: измените `API_PORT` в `.env`
- Frontend: измените в `frontend/vite.config.ts`

### База данных не подключается
1. Проверьте, что Docker контейнеры запущены: `docker-compose ps`
2. Проверьте логи: `docker-compose logs postgres`
3. Проверьте параметры в `.env`:
   - `DB_HOST=localhost`
   - `DB_PORT=5432`
   - `DB_NAME=beauty_db`
   - `DB_USER=beauty_user`
   - `DB_PASSWORD` должен совпадать с паролем в docker-compose.yml

---

## 📱 Mini App (Web App кнопка)

Для работы кнопки "🚀 Открыть приложение" нужен HTTPS:

1. **Скачайте ngrok:** https://ngrok.com/download
2. **Запустите:** `ngrok http 5173`
3. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)
4. **Добавьте в `.env`:**
   ```env
   WEBAPP_URL=https://abc123.ngrok.io
   ```
5. **Перезапустите проект:** `./stop.sh && ./launch.sh`

Подробнее: [`docs/setup_ngrok.md`](docs/setup_ngrok.md)

---

## 🆚 Отличия от Windows версии

- ✅ Используйте `./launch.sh` вместо `LAUNCH.cmd`
- ✅ Используйте `./stop.sh` вместо `STOP.cmd`
- ✅ Python команды: `python3` вместо `python`
- ✅ Пути: `/` вместо `\`
- ✅ Активация venv: `source venv/bin/activate` вместо `venv\Scripts\activate`

---

**Сделано с ❤️ для бьюти-мастеров**

