# 🔧 Как открыть DevTools в Telegram WebApp

## Проблема
В Telegram WebApp стандартная клавиша **F12** не открывает DevTools браузера.

## ✅ Решения

### 1. Использование кнопки DevTools (рекомендуется)

В режиме разработки (localhost или с параметром `?dev=true`) в правом нижнем углу появится кнопка **🔧 DevTools**.

**Как показать кнопку:**
- Откройте приложение на `localhost:5173` (автоматически показывается)
- Или добавьте `?dev=true` к URL: `https://your-app.vercel.app?dev=true`

**Горячие клавиши:**
- `Alt + D` - показать инструкции по открытию DevTools
- `Ctrl + Shift + I` (Windows/Linux) или `Cmd + Option + I` (Mac) - стандартные горячие клавиши браузера

### 2. Открытие в браузере напрямую

**Способ 1: Скопировать URL из Telegram**
1. Откройте приложение в Telegram
2. Нажмите на три точки (меню) в правом верхнем углу
3. Выберите "Открыть в браузере" или скопируйте URL
4. Откройте этот URL в обычном браузере
5. Нажмите `F12` или `Ctrl+Shift+I` для открытия DevTools

**Способ 2: Использовать консоль**
1. Откройте приложение в Telegram
2. В консоли браузера (если доступна) выполните:
   ```javascript
   window.open(window.location.href, '_blank')
   ```
3. В новом окне откройте DevTools стандартным способом

### 3. Удаленная отладка (для мобильных устройств)

**Android:**
1. Подключите устройство к компьютеру через USB
2. Включите "Отладка по USB" в настройках разработчика
3. Откройте Chrome на компьютере: `chrome://inspect`
4. Найдите ваше устройство и нажмите "inspect"

**iOS (только Safari):**
1. Включите "Web Inspector" в настройках Safari на iPhone
2. Подключите iPhone к Mac
3. Откройте Safari на Mac: Develop → [Ваше устройство] → [Ваш сайт]

### 4. Использование параметра URL

Добавьте `?dev=true` к URL приложения для активации режима разработки:
```
https://your-app.vercel.app?dev=true
```

Это покажет кнопку DevTools и дополнительные инструменты отладки.

## 🛠️ Альтернативные инструменты отладки

### React DevTools
Установите расширение React DevTools для браузера:
- [Chrome Extension](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)
- [Firefox Extension](https://addons.mozilla.org/en-US/firefox/addon/react-devtools/)

### Vite DevTools
При разработке на `localhost:5173` доступны:
- Hot Module Replacement (HMR)
- Быстрая перезагрузка при изменениях
- Source maps для отладки

### Console.log
Все логи выводятся в консоль браузера. Используйте:
```javascript
console.log('Отладочная информация')
console.error('Ошибка')
console.warn('Предупреждение')
```

## 📝 Примечания

- В Telegram WebApp некоторые функции DevTools могут быть ограничены
- Для полной функциональности рекомендуется открывать приложение в браузере
- Кнопка DevTools автоматически скрывается в продакшене (не на localhost)

## 🔍 Проверка работы

1. Откройте приложение на `localhost:5173`
2. Убедитесь, что видна кнопка **🔧 DevTools** в правом нижнем углу
3. Нажмите на кнопку или используйте `Alt + D`
4. Следуйте инструкциям для открытия DevTools

