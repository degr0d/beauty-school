# 🧪 Руководство по тестированию

## ✅ ЧТО ГОТОВО:

### 1. **Telegram Бот** 
- ✅ Регистрация пользователей (с сохранением в БД)
- ✅ Команда `/start` - приветствие и главное меню
- ✅ Команда `/help` - справка по командам
- ✅ Команда `/profile` - профиль пользователя
- ✅ Команда `/courses` - список всех курсов
- ✅ Команда `/stats` - статистика обучения (базовая)

### 2. **FastAPI Backend**
- ✅ API для курсов, уроков, профиля, прогресса
- ✅ Swagger UI для тестирования API
- ✅ Endpoints готовы к подключению фронтенда

### 3. **База данных**
- ✅ PostgreSQL настроен и работает
- ✅ Таблицы созданы (миграции)
- ✅ Тестовые курсы загружены

---

## 🚀 КАК ЗАПУСТИТЬ:

### Вариант 1: Всё сразу (РЕКОМЕНДУЕТСЯ)
**Windows:** Двойной клик на `START_ALL.cmd`

**Linux/Mac:**
```bash
python run_all.py &
cd frontend && npm run dev
```

### Вариант 2: Раздельно

**Только Бот + API:**
```bash
python run_all.py
```

**Только Frontend:**
```bash
cd frontend
npm run dev
```

---

## 📋 ЧТО ТЕСТИРОВАТЬ В БОТЕ:

### Тест 1: Регистрация (для нового пользователя)
1. Открой бота в Telegram
2. Напиши `/start`
3. Нажми "Присоединиться"
4. Согласись с обработкой данных
5. Введи ФИО (например: Иванов Иван Иванович)
6. Введи телефон или нажми "Поделиться контактом"
7. ✅ Должен увидеть сообщение "Регистрация завершена!"

### Тест 2: Команды бота
После регистрации протестируй команды:

**`/help`** - должен показать список всех команд

**`/profile`** - должен показать:
- Твоё ФИО
- Телефон
- Username
- Баллы (0 по умолчанию)
- Дату регистрации

**`/courses`** - должен показать список курсов по категориям:
- 💅 Маникюр и педикюр
- 👁 Ресницы и брови
- 🦶 Подология
- 💼 Своё дело

**`/stats`** - показывает базовую статистику

### Тест 3: Повторный `/start`
1. Напиши `/start` снова
2. ✅ Бот должен узнать тебя и поприветствовать по имени
3. ❌ НЕ должен просить регистрироваться заново

---

## 🔍 КАК ПРОВЕРИТЬ ДАННЫЕ В БД:

### После регистрации выполни:
```bash
python check_users.py
```

Должен увидеть:
- Твой Telegram ID
- Username
- ФИО
- Телефон
- Дату регистрации

---

## 🌐 КАК ПРОТЕСТИРОВАТЬ API:

### 1. Запусти API:
```bash
python run_api.py
```

### 2. Открой в браузере:
http://localhost:8000/api/docs

### 3. Протестируй эндпоинты:

**GET `/api/courses`** - получить все курсы
- Нажми "Try it out" → "Execute"
- ✅ Должен вернуть список курсов

**GET `/api/courses/{id}`** - получить курс по ID
- Попробуй с id=1
- ✅ Должен вернуть детали курса + список уроков

**GET `/health`** - проверка здоровья API
- ✅ Должен вернуть: `{"status": "ok", "environment": "development"}`

---

## 🖥️ КАК ПРОТЕСТИРОВАТЬ FRONTEND:

### 1. Запусти фронтенд:
```bash
cd frontend
npm run dev
```

### 2. Открой в браузере:
http://localhost:5173

### 3. Протестируй страницы:
- **Главная** - должны отображаться курсы
- **Каталог** - фильтрация по категориям
- **Профиль** - данные пользователя
- **Сообщества** - список сообществ

---

## 🎯 ПОЛНЫЙ ФЛОУ (E2E тест):

1. ✅ Зарегистрируйся через бота `/start`
2. ✅ Открой Mini App (http://localhost:5173)
3. ✅ Просмотри список курсов
4. ✅ Выбери курс
5. ✅ Начни урок
6. ✅ Отметь урок как пройденный
7. ✅ Проверь прогресс в профиле

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

### Бот не отвечает:
```bash
# Windows:
Stop-Process -Name python -Force
python run_bot_full.py

# Linux/Mac:
pkill python
python run_bot_full.py
```

### Frontend не загружается:
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### API не отвечает:
```bash
# Проверь что процесс запущен
# Windows:
tasklist | findstr python

# Linux/Mac:
ps aux | grep python

# Перезапусти API
python run_api.py
```

### База данных недоступна:
```bash
# Проверь Docker контейнеры
docker-compose ps

# Перезапусти БД
docker-compose restart postgres
```

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ:

```bash
# Остановить все процессы
# Windows:
Stop-Process -Name python,node -Force

# Linux/Mac:
pkill python && pkill node

# Проверить что запущено
# Windows:
tasklist | findstr "python node"

# Linux/Mac:
ps aux | grep "python\|node"

# Очистить логи
# Windows:
del *.log

# Linux/Mac:
rm *.log

# Проверить порты
# Windows:
netstat -ano | findstr "8000 5173"

# Linux/Mac:
lsof -i :8000,5173
```

---

## ✨ ЧЕКЛИСТ ПЕРЕД РЕЛИЗОМ:

- [ ] Все тесты бота проходят
- [ ] API отдаёт корректные данные
- [ ] Frontend отображается правильно
- [ ] Регистрация работает
- [ ] Данные сохраняются в БД
- [ ] Mini App открывается из Telegram
- [ ] Все команды бота работают
- [ ] Нет ошибок в логах
- [ ] Курсы отображаются корректно
- [ ] Прогресс сохраняется

---

## 📊 МЕТРИКИ УСПЕХА:

Тест считается успешным если:
- ✅ Регистрация < 30 секунд
- ✅ API отвечает < 200ms
- ✅ Frontend загружается < 2 секунд
- ✅ Нет ошибок 500
- ✅ Все CRUD операции работают

---

**Удачного тестирования! 🐛**

