# 🔧 Решение проблем Mini App

## ❌ Проблема: Mini App показывает ошибку загрузки

**Симптомы:**
- Android robot icon (страница не загрузилась)
- SSL error (не может установить безопасное соединение)
- "Blocked request. This host is not allowed"

---

## 🔍 Причина

**Mini App требует HTTPS**, а сейчас:
- `WEBAPP_URL` установлен на `http://localhost:5173` (HTTP)
- Нет HTTPS доступа к Frontend

---

## ✅ Решение

### Для локальной разработки

Используйте браузер для разработки:
- Frontend автоматически откроется в браузере на `http://localhost:5173`
- HTTPS не нужен для локальной разработки

### Для тестирования Mini App через Telegram

Используйте Vercel (рекомендуется):
1. Задеплойте frontend на Vercel (см. `VERCEL_DEPLOY.md`)
2. Получите HTTPS URL (например: `https://beauty-school.vercel.app`)
3. Обновите `.env`: `WEBAPP_URL=https://beauty-school.vercel.app`
4. Перезапустите бота

Или используйте временный туннель:
```bash
# ngrok (если установлен)
ngrok http 5173

# Скопируйте HTTPS URL и обновите .env
WEBAPP_URL=https://xxxxx.ngrok-free.app
```

---

## Проверка

После обновления `.env` проверьте:
```bash
cat .env | grep WEBAPP_URL
```

Должен быть HTTPS URL (не `http://localhost:5173`)

---

## Troubleshooting

### Telegram Mini App не открывается

**Решение:**
1. Проверьте что `WEBAPP_URL` в `.env` указывает на HTTPS URL
2. Перезапустите бота
3. Проверьте что URL начинается с `https://`

### Backend не доступен

**Решение:** Backend должен быть доступен по HTTPS. Для продакшена используйте:
- Railway
- Render
- Fly.io
- Или свой сервер с SSL
