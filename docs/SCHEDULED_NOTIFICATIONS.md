# 📱 Напоминания о незавершенных курсах

## Что это?

Это функция, которая **автоматически отправляет напоминания** пользователям через Telegram бота, если они:
- Начали курс, но не завершили его
- Не заходили в курс более **7 дней**

## Как это работает?

1. Скрипт проверяет базу данных
2. Находит пользователей, которые не были активны более 7 дней
3. Отправляет им сообщение в Telegram: "⏰ Вы не заходили в курс [Название] уже 7 дней. Продолжите обучение!"

## Как запустить?

### Вариант 1: Запуск вручную (для тестирования)

```bash
# Из корневой директории проекта
python scripts/send_scheduled_notifications.py
```

Это отправит напоминания всем пользователям, которые не были активны более 7 дней.

### Вариант 2: Автоматический запуск (по расписанию)

**Cron** - это планировщик задач в Linux/Mac, который может запускать команды автоматически.

#### На Mac/Linux:

1. Откройте планировщик задач:
```bash
crontab -e
```

2. Добавьте строку для ежедневного запуска в 10:00:
```bash
0 10 * * * cd /путь/к/проекту && /usr/bin/python3 scripts/send_scheduled_notifications.py >> /tmp/notifications.log 2>&1
```

**Объяснение:**
- `0 10 * * *` = каждый день в 10:00
- `cd /путь/к/проекту` = перейти в директорию проекта
- `>> /tmp/notifications.log` = сохранить логи в файл

#### На Railway (продакшн):

Railway не поддерживает cron напрямую, но можно:

1. **Использовать внешний сервис** (например, [cron-job.org](https://cron-job.org)):
   - Зарегистрируйтесь на cron-job.org
   - Создайте задачу, которая делает HTTP запрос к вашему API
   - Установите расписание (например, раз в день)

2. **Добавить API эндпоинт** для запуска (см. ниже)

### Вариант 3: API эндпоинт (рекомендуется)

Можно добавить API эндпоинт, который будет запускать напоминания. Тогда можно:
- Запускать вручную через админ-панель
- Настроить внешний сервис для автоматического вызова

## Пример использования

```bash
# Запуск вручную
python scripts/send_scheduled_notifications.py

# Вывод:
# 🚀 Запуск отправки запланированных уведомлений...
# ✅ Scheduled reminders completed: {'sent': 5, 'failed': 0, 'total': 10}
# ✅ Готово!
```

## Что происходит при запуске?

1. Скрипт подключается к базе данных
2. Ищет всех пользователей с незавершенными курсами
3. Проверяет, когда они последний раз были активны
4. Если прошло более 7 дней - отправляет напоминание через Telegram бота
5. Выводит статистику: сколько отправлено, сколько не удалось

## Важно!

- Скрипт использует `BOT_TOKEN` из `.env` для отправки сообщений
- Убедитесь, что бот не заблокирован пользователями
- Рекомендуется запускать **раз в день** (не чаще, чтобы не спамить)

## Нужна помощь?

Если хотите, могу:
1. Добавить API эндпоинт для запуска через админ-панель
2. Интегрировать в основной процесс (запуск автоматически при старте API)
3. Добавить настройку интервала (не только 7 дней, но и другие значения)


