# 🚀 Быстрый старт

## ✅ Автоматический запуск через F5 (рекомендуется)

**Всё уже настроено!** Просто нажмите **F5** в VS Code.

### Что происходит автоматически:

1. ✅ **Останавливаются старые процессы** (если запущены)
2. ✅ **Проверяется Docker** и запускаются контейнеры (если не запущены)
3. ✅ **Запускается Backend** (Бот + API на порту 8000)
4. ✅ **Запускается Frontend** (Vite на порту 5173)
5. ✅ **Открывается браузер** автоматически

### Шаг 1: Выберите конфигурацию

В VS Code:
1. Нажмите `Cmd+Shift+D` (панель Run and Debug)
2. В выпадающем списке сверху выберите:

   **🚀 START ALL (F5)** ← **Рекомендуется!**
   - Запустит Backend + Frontend
   - Всё автоматически

### Шаг 2: Нажмите F5

Всё! Проект запустится автоматически.

---

## 📋 Ручная настройка (если нужно)

Этот гайд поможет вам запустить проект локально за 10 минут.

---

## 📋 Предварительные требования

Установите на свой компьютер:
- **Python 3.11+** ([скачать](https://www.python.org/downloads/))
- **Node.js 18+** ([скачать](https://nodejs.org/))
- **Docker** и **Docker Compose** ([скачать](https://www.docker.com/get-started))
- **Git** ([скачать](https://git-scm.com/downloads))

---

## 🔧 Шаг 1: Клонирование проекта

```bash
# Клонируйте репозиторий
git clone <URL_ВАШЕГО_РЕПО>
cd beauty

# Или если вы уже в папке проекта:
# (ничего делать не нужно)
```

---

## 🐳 Шаг 2: Запуск базы данных

```bash
# Запускаем PostgreSQL и Redis через Docker
docker-compose up -d

# Проверяем, что контейнеры запущены
docker-compose ps
```

**Результат:**
```
NAME                  COMMAND                  STATUS
beauty_postgres       "docker-entrypoint.s…"   Up 2 seconds
beauty_redis          "docker-entrypoint.s…"   Up 2 seconds
```

---

## 🐍 Шаг 3: Настройка Backend

### 3.1 Создайте .env файл

```bash
# Скопируйте пример конфигурации
cp config.example .env
```

### 3.2 Заполните .env файл

Откройте `.env` в текстовом редакторе и заполните **обязательные** параметры:

```env
# Получите токены у @BotFather в Telegram
BOT_TOKEN=ВАШ_ТОКЕН_ОСНОВНОГО_БОТА
ADMIN_BOT_TOKEN=ВАШ_ТОКЕН_АДМИН_БОТА

# Ваш Telegram User ID (узнайте у @userinfobot)
ADMIN_IDS=ВАШ_TELEGRAM_ID

# Для локальной разработки остальное можно оставить по умолчанию
DB_PASSWORD=beauty_password_dev
```

### 3.3 Установите зависимости Python

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3.4 Примените миграции БД

```bash
# Создайте таблицы в БД
alembic upgrade head
```

### 3.5 Заполните БД тестовыми данными

```bash
# Запустите скрипт создания тестовых курсов
python -m backend.database.seed_data
```

**Результат:**
```
🌱 Начинаем заполнение БД тестовыми данными...

✅ Создан курс: Основы маникюра для начинающих (5 уроков)
✅ Создан курс: Наращивание ресниц: классика (4 урока)
...
✅ Готово! База данных заполнена.
```

---

## ⚛️ Шаг 4: Настройка Frontend

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Создайте .env файл для frontend
echo "VITE_API_URL=http://localhost:8000/api" > .env

# Вернитесь в корень проекта
cd ..
```

---

## 🎯 Шаг 5: Запуск приложения

Откройте **3 терминала** (или используйте tmux/screen):

### Терминал 1: Backend (Боты + API)

```bash
# Активируйте venv (если ещё не активировано)
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Запустите backend
python backend/main.py
```

**Вы должны увидеть:**
```
==================================================
Beauty School Backend - Запуск
==================================================
Инициализация базы данных...
База данных готова
Запуск основного Telegram-бота...
Запуск админ-бота...
Запуск FastAPI на порту 8000...
```

### Терминал 2: Frontend (React)

```bash
cd frontend
npm run dev
```

**Вы должны увидеть:**
```
  VITE v5.3.4  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## ✅ Шаг 6: Проверка работы

### 1. Проверьте, что боты работают

Откройте Telegram и найдите своих ботов:
- Основной бот: напишите `/start`
- Админ-бот: напишите `/start` и `/stats`

### 2. Откройте фронтенд

Перейдите в браузере: http://localhost:5173/

Вы должны увидеть главную страницу с курсами.

### 3. Проверьте API

Перейдите: http://localhost:8000/api/docs

Вы увидите Swagger UI с документацией API.

---

## 🎉 Готово!

Теперь всё работает! Вы можете:
- ✅ Зарегистрироваться через бота
- ✅ Просматривать курсы в Mini App
- ✅ Смотреть аналитику в админ-боте

---

## 🐛 Частые проблемы

### Ошибка: "ModuleNotFoundError"

**Решение:**
```bash
# Убедитесь, что venv активирован
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate  # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "Can't connect to database"

**Решение:**
```bash
# Проверьте, запущен ли PostgreSQL
docker-compose ps

# Если не запущен:
docker-compose up -d postgres
```

### Ошибка: "Telegram Bot Token is invalid"

**Решение:**
- Убедитесь, что вы правильно скопировали токен из @BotFather
- Проверьте, что в `.env` файле нет лишних пробелов
- Токен должен выглядеть так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### Frontend не загружается (белый экран)

**Решение:**
```bash
cd frontend

# Очистите кеш и переустановите
rm -rf node_modules package-lock.json
npm install

# Перезапустите dev-сервер
npm run dev
```

---

## 📚 Что дальше?

1. **Изучите документацию:**
   - [Архитектура проекта](architecture.md)
   - [Схема базы данных](database.md)
   - [План разработки MVP](mvp-roadmap.md)

2. **Начните разработку:**
   - Добавьте свои курсы через скрипт `seed_data.py`
   - Настройте свой дизайн во `frontend/src/styles/global.css`
   - Добавьте новые фичи согласно roadmap

3. **Подготовьте к деплою:**
   - Следуйте гайду [Deployment Guide](deployment.md)
   - Зарегистрируйте домен
   - Настройте VPS

---

## 💬 Нужна помощь?

Если у вас возникли проблемы:
1. Проверьте логи: `python backend/main.py` покажет ошибки
2. Загляните в Issues проекта
3. Напишите в Telegram (если есть)

---

## 🎓 Полезные команды

```bash
# Остановить Docker контейнеры
docker-compose down

# Посмотреть логи БД
docker-compose logs postgres

# Очистить БД (ОСТОРОЖНО: удалит все данные!)
docker-compose down -v

# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Обновить зависимости
pip freeze > requirements.txt

# Собрать фронтенд для продакшена
cd frontend && npm run build
```

---

**Удачи в разработке! 🚀**

